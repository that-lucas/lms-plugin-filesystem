# Filesystem E2E Fixture

This directory is the deterministic target for LM Studio e2e tests.
Use readonly filesystem tools against it during smoke coverage.

Fixture markers:
- README_SENTINEL
- ALPHA_MARKER
